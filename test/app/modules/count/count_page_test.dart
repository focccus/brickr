import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/count/count_page.dart';

main() {
  testWidgets('CountPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CountPage(title: 'Count')));
    //  final titleFinder = find.text('Count');
    //  expect(titleFinder, findsOneWidget);
  });
}
