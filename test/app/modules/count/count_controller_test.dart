import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:brickr/app/modules/count/count_controller.dart';
import 'package:brickr/app/modules/count/count_module.dart';

void main() {
  initModule(CountModule());
  // CountController count;
  //
  setUp(() {
    //     count = CountModule.to.get<CountController>();
  });

  group('CountController Test', () {
    //   test("First Test", () {
    //     expect(count, isInstanceOf<CountController>());
    //   });

    //   test("Set Value", () {
    //     expect(count.value, equals(0));
    //     count.increment();
    //     expect(count.value, equals(1));
    //   });
  });
}
