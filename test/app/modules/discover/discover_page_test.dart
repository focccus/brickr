import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/discover/discover_page.dart';

main() {
  testWidgets('DiscoverPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DiscoverPage(title: 'Discover')));
    //  final titleFinder = find.text('Discover');
    //  expect(titleFinder, findsOneWidget);
  });
}
