import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:brickr/app/modules/discover/discover_controller.dart';
import 'package:brickr/app/modules/discover/discover_module.dart';

void main() {
  initModule(DiscoverModule());
  // DiscoverController discover;
  //
  setUp(() {
    //     discover = DiscoverModule.to.get<DiscoverController>();
  });

  group('DiscoverController Test', () {
    //   test("First Test", () {
    //     expect(discover, isInstanceOf<DiscoverController>());
    //   });

    //   test("Set Value", () {
    //     expect(discover.value, equals(0));
    //     discover.increment();
    //     expect(discover.value, equals(1));
    //   });
  });
}
