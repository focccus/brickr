import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:brickr/app/modules/options/options_controller.dart';
import 'package:brickr/app/modules/options/options_module.dart';

void main() {
  initModule(OptionsModule());
  // OptionsController options;
  //
  setUp(() {
    //     options = OptionsModule.to.get<OptionsController>();
  });

  group('OptionsController Test', () {
    //   test("First Test", () {
    //     expect(options, isInstanceOf<OptionsController>());
    //   });

    //   test("Set Value", () {
    //     expect(options.value, equals(0));
    //     options.increment();
    //     expect(options.value, equals(1));
    //   });
  });
}
