import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/options/options_page.dart';

main() {
  testWidgets('OptionsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OptionsPage(title: 'Options')));
    //  final titleFinder = find.text('Options');
    //  expect(titleFinder, findsOneWidget);
  });
}
