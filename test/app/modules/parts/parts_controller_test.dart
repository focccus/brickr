import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:brickr/app/modules/parts/parts_controller.dart';
import 'package:brickr/app/modules/parts/parts_module.dart';

void main() {
  initModule(PartsModule());
  // PartsController parts;
  //
  setUp(() {
    //     parts = PartsModule.to.get<PartsController>();
  });

  group('PartsController Test', () {
    //   test("First Test", () {
    //     expect(parts, isInstanceOf<PartsController>());
    //   });

    //   test("Set Value", () {
    //     expect(parts.value, equals(0));
    //     parts.increment();
    //     expect(parts.value, equals(1));
    //   });
  });
}
