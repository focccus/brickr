import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/parts/parts_page.dart';

main() {
  testWidgets('PartsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PartsPage(title: 'Parts')));
    //  final titleFinder = find.text('Parts');
    //  expect(titleFinder, findsOneWidget);
  });
}
