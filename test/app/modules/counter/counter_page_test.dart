import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/counter/counter_page.dart';

main() {
  testWidgets('CounterPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CounterPage(title: 'Counter')));
    //  final titleFinder = find.text('Counter');
    //  expect(titleFinder, findsOneWidget);
  });
}
