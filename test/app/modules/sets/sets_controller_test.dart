import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:brickr/app/modules/sets/sets_controller.dart';
import 'package:brickr/app/modules/sets/sets_module.dart';

void main() {
  initModule(SetsModule());
  // SetsController sets;
  //
  setUp(() {
    //     sets = SetsModule.to.get<SetsController>();
  });

  group('SetsController Test', () {
    //   test("First Test", () {
    //     expect(sets, isInstanceOf<SetsController>());
    //   });

    //   test("Set Value", () {
    //     expect(sets.value, equals(0));
    //     sets.increment();
    //     expect(sets.value, equals(1));
    //   });
  });
}
