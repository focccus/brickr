import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:brickr/app/modules/sets/sets_page.dart';

main() {
  testWidgets('SetsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SetsPage(title: 'Sets')));
    //  final titleFinder = find.text('Sets');
    //  expect(titleFinder, findsOneWidget);
  });
}
